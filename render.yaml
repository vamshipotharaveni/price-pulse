services:
  - type: web
    name: price-pulse-backend
    env: node
    plan: starter
    buildCommand: "npm run build"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
    repo: ""
    branch: main
    autoDeploy: true
